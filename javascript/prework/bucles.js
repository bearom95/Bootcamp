// BUucles para recorrer cosas

let elements = ["a", "b", "c", "d", "e"];

for (let index = 0; index < elements.length; index++) {
    console.log(index);
    if (elements[index] === "c") {
        console.log("Es una C");
    }
    console.log(elements[index]);
}

